## Agent skills

### Issue tracker

Tickets are local Markdown files managed with `kt` under `.ktickets/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the five standard triage states recorded in ticket metadata. See `docs/agents/triage-labels.md`.

### Domain docs

This repository uses a single-context domain-document layout. See `docs/agents/domain.md`.
